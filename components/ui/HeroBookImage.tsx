/** Back-compat re-export — implementation lives in `./book/`. */
export type {
  BookImageProps,
  HeroBookImageProps,
} from "@/components/ui/book";
export { BookImage, HeroBookImage } from "@/components/ui/book";
